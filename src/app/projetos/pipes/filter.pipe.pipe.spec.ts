import { Filter.PipePipe } from './filter.pipe';

describe('Filter.PipePipe', () => {
  it('create an instance', () => {
    const pipe = new Filter.PipePipe();
    expect(pipe).toBeTruthy();
  });
});
